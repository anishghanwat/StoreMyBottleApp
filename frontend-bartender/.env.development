# Development Environment Variables
# Used when running: npm run dev

# This will be overridden by docker-compose environment variable if set
VITE_API_URL=${VITE_API_URL:-http://localhost:8000}

# Sentry (leave empty in dev — only active in production builds)
VITE_SENTRY_DSN=
